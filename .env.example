# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=madness

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@madness.com
PGADMIN_DEFAULT_PASSWORD=admin123

# Backend settings (example keys)
DB_HOST=db
DB_PORT=5432
DB_USER=${POSTGRES_USER}
DB_PASS=${POSTGRES_PASSWORD}
DB_NAME=${POSTGRES_DB}

# Auth (placeholders; set real values later or leave blank for DEV_AUTH)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
DEV_AUTH=true
